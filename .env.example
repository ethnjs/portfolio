# A random string (32+ chars) used to derive the AES-256 encryption key.
ENCRYPTION_SECRET=your-long-random-secret-here

# JSON array of valid access keys and their expiry dates.
# Generate entries with: pnpm generate-key
PORTFOLIO_KEYS=[{"key":"your_plain_key","expires":"YYYY-MM-DD"}]
